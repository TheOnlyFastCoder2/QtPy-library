{
  "scripts": {
    "build:types": "tsc --emitDeclarationOnly --outDir dist-types --declaration",
    "prepublishOnly": "npm run build && npm run build:types",
    "build:all": "npm run build -w @qtpy/*"
  },
  "peerDependencies": {
    "typescript": ">=5.0.0"
  },
  "devDependencies": {
    "typescript": "^5.0.0",
    "vite": "^4.0.0"
  }
}
